expose_php = Off

; Explicit rather than relying on the base image's default (128M): gives a
; predictable ceiling for image-processing requests (GD/Intervention Image
; decode a full bitmap into memory before resizing) regardless of what
; future base-image bumps ship as their own default.
memory_limit = 256M
