<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="./vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory suffix="Test.php">./tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory suffix="Test.php">./tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory suffix=".php">./app</directory>
        </include>
        <exclude>
            <!-- Standalone scraper commands that hit real third-party sites
                 over HTTP; covering them meaningfully needs an HTTP-mock
                 harness that's a separate effort from the coverage push
                 here, and they're not part of the request-handling/security
                 surface the rest of this suite targets. -->
            <directory suffix=".php">./app/Console/Commands</directory>
        </exclude>
    </source>
    <php>
        <!-- The dev DB this suite runs against (no sqlite :memory: override, see below)
             holds thousands of real seeded places/ballades/hebergements; several
             *ControllerTest index tests cache-serialize that whole listing in this
             single long-running process (CACHE_DRIVER=array never releases it between
             tests), which already sat within a few MB of PHP's default 128M CLI
             memory_limit. Give the suite headroom instead of trimming coverage. -->
        <ini name="memory_limit" value="256M"/>
        <env name="APP_ENV" value="testing"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_DRIVER" value="array"/>
        <!-- <env name="DB_CONNECTION" value="sqlite"/> -->
        <!-- <env name="DB_DATABASE" value=":memory:"/> -->
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
    </php>
</phpunit>
